import styles from "./Playlist.module.css";

const SignupScreen = () => (
  <div className="App">
    <header className="App-header">
      <p>Signup page</p>
    </header>
  </div>
);

export default SignupScreen;
