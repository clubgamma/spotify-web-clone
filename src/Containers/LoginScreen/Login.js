import styles from "./Login.module.css";

const LoginScreen = () => (
  <div className="App">
    <header className="App-header">
      <p>Login page</p>
    </header>
  </div>
);

export default LoginScreen;
