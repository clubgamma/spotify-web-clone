import styles from "./Playlist.module.css";

const Playlist = () => (
  <div className="App">
    <header className="App-header">
      <p>Playlist page</p>
    </header>
  </div>
);

export default Playlist;
